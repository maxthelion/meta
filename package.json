{
  "name": "meta",
  "version": "0.1.0",
  "description": "Central command hub for projects under ~/dev",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "install-pm-loop": "bun run src/cli/install-pm-loop.ts"
  },
  "bin": {
    "meta-install-pm-loop": "src/cli/install-pm-loop.ts"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
