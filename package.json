{
  "name": "meta",
  "version": "0.1.0",
  "description": "Central command hub for projects under ~/dev",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "pm-loop": "bun run src/cli/pm-loop.ts"
  },
  "bin": {
    "meta-pm-loop": "src/cli/pm-loop.ts"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
